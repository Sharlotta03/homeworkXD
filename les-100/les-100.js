// const numbers = [1, 3, 5, 10, 21, 24]

// for(let i = 0; i < numbers.length; i++) {
//   if (numbers[i] % 2 == 0) {
//     console.log(numbers[i])
//   }
// }

const colors = ['red', 'orange', 'yeallow', 'green', 'blue', 'indigo', 'violet']

for(let i = colors.length  - 1; i >= 0; i--) {
  console.log(colors[i])
}